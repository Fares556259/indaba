name: adaptive-sentinel-defense
kind: defense
api_version: v1
team: sentinel-defense-team
port: 8080
models: []          # declare every external pretrained model: {name, source, license}
datasets: []        # declare every external dataset: {name, source, license}
resources:
  memory: 1g
  cpus: 1.0
  gpus: 0
description: Adaptive multi-tier defense featuring recursive bounded normalizer, taint tracking, cross-record corpus matching, and memory write protection.

